/**
 * PDF Font Registration
 * 
 * Using standard PDF fonts (Helvetica) instead of external fonts
 * to avoid external dependencies and network requests.
 * 
 * Standard fonts available in @react-pdf/renderer:
 * - Helvetica (default)
 * - Times-Roman
 * - Courier
 */
export const registerPDFFonts = () => {
  // No font registration needed - using standard Helvetica font
  // This avoids external CDN requests and ensures consistent rendering
};
